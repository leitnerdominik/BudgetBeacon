BudgetBeacon Production TODO
===========================

Critical before production
--------------------------
[x] Replace demo authentication with real user management.
    - Remove DemoAuth-based login from `BudgetBeacon.Api/Controllers/AuthController.cs`.
    - Remove demo credentials from `BudgetBeacon.Api/appsettings.Development.json`.
    - Remove the demo credentials hint from `budgetbeacon-front/src/features/auth/components/LoginPage.tsx`.

[x] Implement real API authentication and authorization.
    - Add JWT or secure session auth in the backend.
    - Register `UseAuthentication()` and protect private endpoints with `[Authorize]`.
    - Validate tokens on every protected API route, not only in the frontend.

[ ] Remove secrets and local credentials from source control.
    - Move `ConnectionStrings:DefaultConnection` and `OpenRouter:ApiKey` out of `BudgetBeacon.Api/appsettings.json`.
    - Use environment variables, user secrets, vault/secret manager and separate production config.
    - Rotate any exposed AI provider API key and database password.

[ ] Replace development-only CORS and API endpoint assumptions.
    - Remove hardcoded localhost origins from `BudgetBeacon.Api/Program.cs`.
    - Make frontend API base URL and backend allowed origins environment-driven.
    - Replace the dev proxy target in `budgetbeacon-front/vite.config.ts` with production env config.

[x] Replace `EnsureCreatedAsync()` with proper EF Core migrations.
    - Create and apply database migrations.
    - Add startup strategy for schema updates per environment.
    - Define backup and rollback procedure for production deployments.

High priority
-------------
[x] Add backend input validation, consistent problem details and safer error handling.
    - Return standardized error payloads.
    - Avoid broad `Exception` messages leaking implementation details.
    - Add request size/file type validation for CSV uploads.

[x] Harden frontend auth storage.
    - Avoid storing production auth tokens in `localStorage` if possible.
    - Prefer secure, HTTP-only cookies or another server-backed session strategy.
    - Add token refresh / expiry handling and forced logout on 401.

[x] Add CSRF protection for cookie-based auth.
    - Issue CSRF tokens from the backend for SPA clients.
    - Validate unsafe requests server-side.
    - Send CSRF tokens automatically from the frontend on `POST`, `PUT`, `PATCH` and `DELETE`.

[ ] Add automated tests.
    - Backend: controller/service tests for auth, transactions, import and AI tips.
    - Frontend: component tests for login, dashboard, transactions, tips and notifications.
    - End-to-end: login, CSV import, dashboard refresh, mobile drawer, logout.

[x] Add production observability.
    - Health endpoint / readiness checks.
    - Structured request correlation and error monitoring.
    - Alert-friendly signals for API failures, database failures and AI provider failures.

[ ] Review AI endpoint safety and cost control.
    - Add rate limiting / quotas for AI endpoints.
    - Add timeout/retry policy and fallback UX.
    - Log prompt failures safely without exposing secrets or sensitive user data.

Frontend and mobile polish
--------------------------
[x] Optimize the transactions screen for mobile.
    - `@mui/x-data-grid` with fixed columns is not mobile-friendly in `budgetbeacon-front/src/features/transactions/components/TransactionList.tsx`.
    - Add a mobile list/card layout or responsive column reduction.
    - Improve spacing, typography and touch targets for small screens.

[x] Improve app bar and drawer behavior on small screens.
    - Add a close action and clearer active-state treatment in the mobile drawer.
    - Test account menu and drawer interactions on narrow screens and touch devices.
    - Ensure keyboard and screen reader navigation works.

[x] Make dashboard cards and tips more compact on small screens.
    - Review card heights, padding and typography on 320-390px widths.
    - Avoid long tip text creating very tall cards on phones.
    - Consider collapsible or truncated descriptions with "more" actions.

[x] Fix remaining text/encoding inconsistencies.
    - Replace malformed euro rendering such as `â‚¬`.
    - Standardize currency/date formatting with locale-aware formatters.

[x] Add loading, empty and error UX consistency.
    - Show retry actions for failed data loads.
    - Avoid duplicate error surfaces where toast + inline message are noisy together.
    - Add offline / slow-network states where relevant.

Release engineering
-------------------
[ ] Create environment documentation.
    - Required env vars for frontend and backend.
    - Local/dev/staging/prod setup instructions.
    - Deployment steps and rollback checklist.

[x] Replace the template README with project documentation.
    - Architecture overview.
    - Run/build/deploy instructions.
    - Mobile support expectations and limitations.

[ ] Add CI/CD checks.
    - Frontend lint, typecheck, build.
    - Backend build and test.
    - Optional preview/staging deploy before production release.

[ ] Address frontend bundle size.
    - Current build reports a large JS bundle.
    - Add route-level code splitting and review heavy dependencies like DataGrid.

Nice to have before launch
--------------------------
[ ] Add user profile/settings flow.
[ ] Add password reset and account recovery.
[ ] Add audit trail / admin visibility for imports and AI actions.
[ ] Add accessibility review (contrast, focus states, ARIA, keyboard flow).
[ ] Add PWA/offline strategy only if mobile web usage is a real product goal.
